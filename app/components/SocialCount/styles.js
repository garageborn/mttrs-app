import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  shares: {
    flexDirection: 'row',
    alignItems: 'center'
  },

  shareIcon: {
    width: 8,
    height: 13,
    marginRight: 3,
    marginTop: -1
  },

  text: {
    fontSize: 12,
    fontWeight: '400',
    color: '#666766'
  }
})

export default styles
