import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  buttonContainer: {
    marginTop: 16,
    alignSelf: 'center'
  },

  buttonText: {
    color: '#FF5607',
    fontSize: 24,
    fontWeight: '500'
  }
})

export default styles
