import { StyleSheet } from 'react-native'
const height = 40

const styles = StyleSheet.create({
  container: {
    height,
    paddingHorizontal: 4,
    paddingVertical: 4
  }
})

export default styles
