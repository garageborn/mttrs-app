import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  lightText: {
    fontSize: 12,
    color: '#999'
  },

  darkText: {
    fontSize: 12,
    color: '#666'
  },

  publisher: {
    flexDirection: 'row',
    alignItems: 'center'
  },

  publisherLogo: {
    width: 24,
    height: 24
  }
})

export default styles
