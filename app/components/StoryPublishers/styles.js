import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  lightText: {
    fontSize: 13,
    color: '#999'
  },

  publisher: {
    flexDirection: 'row',
    alignItems: 'center'
  },

  publisherLogo: {
    width: 22,
    height: 22
  }
})

export default styles
