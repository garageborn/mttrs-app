import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  lightText: {
    fontSize: 14,
    color: '#999'
  },

  darkText: {
    fontSize: 14,
    color: '#666766'
  },

  publisher: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 10
  },

  publisherLogo: {
    width: 24,
    height: 24
  },

  restrictContent: {
    flexDirection: 'row',
    alignItems: 'center'
  }
})

export default styles
