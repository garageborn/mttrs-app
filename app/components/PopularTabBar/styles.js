import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    alignItems: 'center'
  },
  text: {
    color: '#999999',
    textAlign: 'center'
  }
})

export default styles
