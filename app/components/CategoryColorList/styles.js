import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  list: {
    flexGrow: 1,
    flexDirection: 'row',
    alignItems: 'flex-end',
    position: 'relative',
    height: 9,
    bottom: 9
  }
})

export default styles
