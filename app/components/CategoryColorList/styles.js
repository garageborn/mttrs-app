import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  list: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    position: 'absolute',
    height: 12,
    bottom: -6,
    zIndex: 1
  }
})

export default styles
