import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  content: {
    flexGrow: 1,
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 3,
    paddingTop: 10,
    paddingRight: 10,
    paddingLeft: 10
  }
})

export default styles
