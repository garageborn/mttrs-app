import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    marginLeft: 10
  },

  text: {
    fontSize: 12,
    fontWeight: '500',
    color: '#999'
  }
})

export default styles
