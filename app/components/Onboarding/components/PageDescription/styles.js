import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  description: {
    fontSize: 14,
    lineHeight: 20,
    marginTop: -5,
    color: '#999'
  }
})

export default styles
