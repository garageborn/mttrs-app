import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  card: {
    position: 'relative',
    zIndex: 2,
    marginTop: 8,
    marginBottom: 8,
    padding: 10,
    backgroundColor: '#FFF'
  }
})

export default styles
