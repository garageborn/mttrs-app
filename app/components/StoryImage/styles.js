import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  content: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 3
  },

  image: {
    width: 120,
    height: 90,
    borderWidth: 2,
    borderColor: '#F1F1F1'
  },

  visited: {
    opacity: 0.7
  }
})

export default styles
