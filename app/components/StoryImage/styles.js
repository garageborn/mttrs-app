import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  content: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 3
  },

  image: {
    width: 120,
    height: 95,
    borderRadius: 2
  },

  visited: {
    opacity: 0.6
  }
})

export default styles
