import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    position: 'relative',
    top: -40,
    right: 20,
    padding: 15,
    marginRight: 20
  },
  text: {
    fontSize: 30,
    width: 6,
    height: 50,
    lineHeight: 8
  }
})

export default styles
