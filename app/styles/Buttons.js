import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  closeButton: {
    alignItems: 'center',
    marginTop: 16
  },

  closeButtonIcon: {
    width: 39,
    height: 39
  }
})

export default styles
