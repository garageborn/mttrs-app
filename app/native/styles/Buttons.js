import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  closeButton: {
    alignItems: 'center'
  },

  closeButtonIcon: {
    width: 39,
    height: 39
  }
})

export default styles
