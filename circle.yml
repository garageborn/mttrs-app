machine:
  node:
    version: 'v6.1.0'
  services:
    - docker

dependencies:
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash
    - gem install bundler
  cache_directories:
    - ~/docker
    - ~/.yarn-cache
  override:
    - yarn install
    - bundle install
  post:
    - PULL_REQUEST_ID=`echo $CI_PULL_REQUEST | grep -o -E '[0–9]+$' | head -1 | sed -e 's/^0\+//'` bundle exec pronto run -f github_pr

test:
  override:
    - echo 'Skip'
