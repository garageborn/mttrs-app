deployment:
  production:
    branch: features/capistrano
    commands:
      - cd web && bundle install
      - cd web && bundle exec cap production deploy branch=features/capistrano

test:
  override:
    - echo 'Skip'
