machine:
  node:
    version: v6.1.0
  services:
    - docker

dependencies:
  cache_directories:
    - /tmp/docker

deployment:
  production:
    branch: master
    commands:
      - bundle exec rake deploy

test:
  override:
    - echo 'Skip'
